spring:
  cloud:
    consul:
      host: consul
      port: 8500
      config:
        enabled: true
        fail-fast: false
  application:
    name: auth-jwt
logging:
  path: logs/${spring.application.name}