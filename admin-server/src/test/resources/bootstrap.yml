spring:
  cloud:
    bus:
      enabled: false
    discovery:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
  application:
    name: admin-server
logging:
  path: logs/${spring.application.name}