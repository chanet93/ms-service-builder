spring:
  cloud:
    consul:
      host: consul
      port: 8500
      config:
        enabled: true
        fail-fast: false
  application:
    name: admin-server
logging:
  path: logs/${spring.application.name}